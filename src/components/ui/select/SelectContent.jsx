import React from 'react';

const SelectContent = ({ children, ...props }) => { 
    return (
        <React.Fragment>
            {children}
        </React.Fragment>
    )
}

export default SelectContent